#include<stdio.h>
void main()
{
    int h,a,p,g;
    double pre,//h=health,a=age,p=place,g=gender
    printf("Enter 1 for female and 2 for male\nYour Gender:");
    scanf("%d",&g);
    printf("Enter 1 for Excellent and 2 for poor Health:");
    scanf("%d",&h);
    printf("Enter your age:");
    scanf("%d",&a);
    printf("Where do you live? \nEnter 1 for city & 2 for village :");
    scanf("%d",&p);

    if(a>=25 && a<=35)
    {
        if(h==1 && p==1)
        {
            if(g==2)
        }
    }
          {






